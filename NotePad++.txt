git config --global user.name "Praveen Gupta"
git config --global user.email "guptaprveenpkg@gmail.com"
git init
git git remote add origin https://github.com/Praveen-30/Banking_Management.git        (it is to sync local to particular repository)
git remote -v (to check if sync is done)
git push --set-upstream origin master             (First push command to any branch ,it will select the branch in repository)
git branch -m master main                (to rename local branch from master to main)
git push -u origin main                  (Push the renamed branch to the remote (like GitHub))
git chekout main/master/laernJava   (to switch between branches)
git checkout -b learnjava         (To create and switch directly to branch)


